'use strict';

module.exports = {
	sourceDir: 'temp/temp/dashboard', // This directory contains code to be inserted into destination app.
	imageDir: 'temp/temp/gm_images', // This directory contains images to be inserted into destination app.
	scriptFile: 'gmScripts.sh', // Contains terminal commands for destination app.
	relativePath: 'client/scripts' //Relative template path in the source
}